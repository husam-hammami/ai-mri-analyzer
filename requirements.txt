fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydicom==2.4.4
# numpy<2 is a HARD constraint: scipy 1.12.0 is built against the numpy 1.26 ABI; numpy 2.x
# crashes every read. 1.26.4 is the version verified against scipy 1.12.0 (matches check_env()).
numpy==1.26.4
scipy==1.12.0
Pillow==10.2.0
anthropic==0.43.0
reportlab==4.1.0
# Multi-format input support — bounded so a transitive resolve can't pull a major bump.
nibabel>=5.2.0,<6
pynrrd>=1.0.0,<2
# Arabic report layer (flag-dark MIKA_AR_ENABLED). PIN python-bidi==0.4.2: it is the LAST
# pure-Python (py3-none-any) release; >=0.5 ships a compiled Rust/maturin wheel that needs a
# per-target binary and would break the hashed Electron bundle lock (same class as the numpy pin).
arabic-reshaper==3.0.0
python-bidi==0.4.2
